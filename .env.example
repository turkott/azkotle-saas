# Postgres (required)
POSTGRES_DB=azkotle
POSTGRES_USER=azkotle
POSTGRES_PASSWORD=change_me

# Pro prod Seq odstraň SEQ_FIRSTRUN_NOAUTHENTICATION z compose a nastav
# SEQ_FIRSTRUN_ADMINPASSWORDHASH — vygeneruj přes:
#   docker run --rm -it datalust/seq config hash
